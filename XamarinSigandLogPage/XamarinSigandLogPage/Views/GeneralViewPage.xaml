<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="XamarinSigandLogPage.Views.GeneralViewPage"
             x:Name="GeneralView">
    <ContentView.Content>
        <StackLayout BindingContext="{x:Reference GeneralView }">  
            
            <Image Style="{StaticResource StyleImage}"
                   Source="{Binding ImageView}" 
                   
                   />
            <Label  Style="{StaticResource StyleLabels}"
                    Text="{Binding TextLabel}" 
                    />
            
            <Label  Style="{StaticResource StyleLabelSmall}"
                    Text="{Binding TextLabelSmall}" 
                    HorizontalOptions="End"
                    VerticalOptions="Center"

                    />
            
        </StackLayout>
    </ContentView.Content>
</ContentView>